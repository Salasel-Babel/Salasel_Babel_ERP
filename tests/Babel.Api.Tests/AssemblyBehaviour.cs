using Xunit.Sdk;
using Xunit.v3;

// ─────────────────────────────────────────────────────────────────────────────
// هذه المجموعة تعمل **بالتسلسل**، ولهذا سبب واحد مقيس لا تفضيل أسلوبي:
//
//     كل اختبار هنا يُقلع **عملية خادم** ويفتح مجمّع اتصالات إلى PostgreSQL. والتوازي
//     يضاعف عدد العمليات وعدد الاتصالات، و`max_connections` على هذا الجهاز = 100.
//
// وكان هنا سببٌ ثانٍ — أن الحِمل الذي يضيفه التوازي يُفشل تهيئة مجموعة أخرى في منتصفها
// فتُعاد تهيئتها، وطريق تعافيها مُدمِّر — و**ذلك السبب زال**: كل مجموعة صارت تملك قاعدة
// بيانات باسم خاصّ بعمليتها، ولا تُسقط شيئاً عند البدء، ولا تُعيد البناء بعد فشل جزئي.
// يُترك الوصف هنا لأن اصطلاحاً يشرح سببه أوضح من واحد يُذكر بلا سبب — لكنه **ليس سبباً
// قائماً**، والسبب الأول وحده هو ما يمنع رفع التوازي.
// (‏docs/evidence/traps.md#fakh-test-databases-share-a-fixed-name-across-processes)
// ─────────────────────────────────────────────────────────────────────────────
[assembly: Parallelization(Mode = ParallelMode.None)]
