/* ═══════════════════════════════════════════════════════════════════════════
   ما يقوله العقد لهذا القسم — مكتوباً مرّةً واحدة  ·  What the contract says
   ───────────────────────────────────────────────────────────────────────────
   **ولا رقمَ حسابٍ واحد في هذا الملفّ ولا في القسم كلّه.** الشاشة لا تعرف
   أرقام الحسابات ولا تكتبها: ما تحمله المستندات **مؤهّلاتُ دور** —
   `itemGroup` و`taxClassification` و`settlementMethod` و`expenseCategory` —
   و**مصفوفة الترحيل في `data/posting-matrix/` وحدها** تحلّها إلى حسابات.
   وإن احتاجت الشاشة أن تُري حساباً فمن **إيصال الترحيل العائد** لا من هنا.

   والقيم أدناه «المستعمَل اليوم» كما ينصّ العقد حرفاً: مجموعاتٌ **مفتوحة
   تُنشر نصّاً لا مجموعاتٍ مغلقة** — فتضييقُها بعد نشرها يفرض v2. ولذلك
   تُعرض قوائمَ اقتراحٍ (`datalist`) لا قوائمَ اختيارٍ مغلقة: قيمةٌ لم تخطر
   لنا اليوم يجب أن تعبر، ورفضُها في المتصفّح يمنع ما يقبله الخادم.
   ═══════════════════════════════════════════════════════════════════════════ */

/* ═══════════════════════════════════════════════ ١ · حالات المستند */

/** مسوّدة — أُنشئت ولم تُرحَّل. ولا أثر لها على ذمّةٍ ولا على مخزون. */
export const DRAFT = "DRAFT";
/** مُرحَّلة — لها قيدٌ في الدفتر. */
export const POSTED = "POSTED";
/** معتمدة — على أمر الشراء وحده، ولا POSTED عليه أبداً. */
export const APPROVED = "APPROVED";
/** معكوسة. */
export const REVERSED = "REVERSED";
/** ملغاة. */
export const CANCELLED = "CANCELLED";

/** الحالات التي تعرف الشاشة أسماءها. وما عداها **يُعرض كما وصل**. */
export const KNOWN_STATES: readonly string[] = [DRAFT, POSTED, APPROVED, REVERSED, CANCELLED];

/* ═══════════════════════════════════ ٢ · مؤهّلات الدور — مفتوحةٌ عمداً */

/** طرق التسوية المستعمَلة اليوم — يحلّها دليل الحسابات إلى خزينة أو بنك. */
export const SETTLEMENT_METHODS: readonly string[] = ["cash", "bank", "card_clearing"];

/** التصنيفات الضريبية المستعمَلة اليوم. */
export const TAX_CLASSIFICATIONS: readonly string[] = ["standard", "zero", "exempt"];

/* ═════════════════════════════════════════════ ٣ · رموز الرفض المنشورة
   **الرمز هو نقطة الاعتماد البرمجية الوحيدة** — ولا يُقرأ نصّ رسالةٍ لاتخاذ
   قرار. وهذه الثلاثة منشورةٌ بأوصافها في العقد، فتُسمّى ويُشرَح ما تعنيه؛
   وما عداها يُعرض بلوحة المشكلة العامّة كما أرسله الخادم. */

/** تخصيصٌ يتجاوز المقبوض أو متبقّي الفاتورة — يُسمّي الرقمين. */
export const SALES_OVER_ALLOCATION = "sales.over_allocation";
/** تخصيصٌ يتجاوز المدفوع أو متبقّي فاتورة المورّد — يُسمّي الرقمين. */
export const PURCHASING_OVER_ALLOCATION = "purchasing.over_allocation";
/** كمّيةٌ مستلمة تتجاوز المطلوب على سطر الأمر — الضلع الأول من المطابقة. */
export const RECEIPT_EXCEEDS_ORDER = "purchasing.receipt_exceeds_order";
