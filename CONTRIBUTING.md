# دليل المساهمة واستراتيجية الفروع

## 1. الفروع

النموذج المعتمد هو **Git Flow** بثلاثة فروع دائمة وفروع عمل قصيرة العمر:

```
production      ما يعمل لدى العميل فعلياً
 ▲
main            المستقر المُختبَر — بوابة المالك، محمي
 ▲
develop         فرع التكامل اليومي
 ▲
feature/<مهمة> · claude/<مهمة> · fix/<مهمة>      فروع العمل
```

| الفرع | الدوام | المالك | القاعدة |
|---|---|---|---|
| `production` | دائم | مالك المستودع | ما يعمل لدى العميل. يستقبل من `main` عند الإصدار فقط. لا دفع مباشر |
| `main` | دائم | مالك المستودع | المستقر المُختبَر وبوابة الاعتماد. يستقبل من `develop` عبر Pull Request بموافقة المالك. لا دفع مباشر |
| `develop` | دائم | الفريق | فرع التكامل اليومي. يستقبل من فروع العمل عبر Pull Request |
| `feature/*` · `claude/*` · `fix/*` | مؤقت | المطوّر أو الوكيل | فروع قصيرة العمر. تتفرع من `develop` وتُدمج فيه، ثم تُحذف |
| `gh-pages` | دائم | آلي | فرع النشر لـ GitHub Pages. يحمل حمولة النشر فقط في جذره ولا يُدمج في أي فرع آخر |

### التدفق

```
feature/<مهمة>  ──PR──▶  develop  ──PR──▶  main  ──إصدار──▶  production
   (المطوّر)          (مراجعة الفريق)   (موافقة المالك)      (تشغيل العميل)
```

**قواعد ملزمة:**
- لا يُدفع إلى `production` ولا `main` مباشرة تحت أي ظرف.
- كل فرع عمل يتفرّع من **أحدث** `develop`.
- الفرع الواحد لمهمة واحدة. لا تُخلط مهام غير مترابطة في فرع واحد.
- عند تعارض الدمج: يُدمج الفرع القاعدة **في** فرع العمل (Merge)، ولا يُعاد كتابة تاريخ فرع يعمل عليه غيرك.
- الصلاحيات تُمنح على `develop` لا بفروع باسم الأشخاص. لا فرع شخصي لأي متعاون.

## 2. رسائل الالتزام (Commits)

بصيغة Conventional Commits:

```
<النوع>(<النطاق>): <وصف موجز>
```

| النوع | الاستخدام |
|---|---|
| `feat` | ميزة جديدة |
| `fix` | إصلاح خلل |
| `docs` | توثيق فقط |
| `refactor` | إعادة هيكلة دون تغيير سلوك |
| `test` | اختبارات |
| `chore` | بنية تحتية وأدوات |

النطاق يطابق اسم الوحدة: `core`, `ledger`, `sales`, `purchasing`, `inventory`, `pos`, `hr`,
`projects`, `realestate`, `assets`, `compliance`, `portals`, `ai`, `docs`.

**أمثلة:**
```
docs(analysis): إضافة الرؤية الأولية ومصفوفة الترحيل
feat(ledger): محرك ترحيل القيود مع التحقق من توازن القيد
fix(compliance): تصحيح ترتيب عناصر UBL في فاتورة الإشعار الدائن
```

## 3. قواعد خاصة بالكود المحاسبي

هذه ليست تفضيلات أسلوبية — هي شروط قبول:

1. **لا وحدة تكتب في دفتر الأستاذ مباشرة.** الترحيل عبر `Ledger.PostingService` حصراً.
2. **لا `float` ولا `double` في أي حقل مالي.** `decimal` في الكود و`NUMERIC(19,4)` في قاعدة البيانات.
3. **كل تغيير يمس منطق ترحيل يرافقه اختبار آلي** يتحقق من القيد الناتج بالضبط.
4. **لا حذف لقيد مرحّل.** التصحيح بقيد عكسي فقط.
5. **كل مسمّى بيانات تأسيسية يحمل اسماً عربياً إلزامياً — والاسم العربي هو السجلّ.**
   والزوج الثابت `name_ar`/`name_en` هو **الحالة القائمة اليوم لا الحالة المقصودة**: هو
   عاجز بنيوياً عن لغة ثالثة، ونقلُه إلى (اسم عربي + جدول ترجمات) مُقرَّر في
   [ADR-0021](docs/decisions/ADR-0021-arabic-is-the-record-translation-is-presentation.md)
   ونطاقه المُعاد قياسه في §4 منه. **فلا يُضاف زوج `ar`/`en` جديد في أي حقل عرض.**
   والاستثناء الوحيد **النصّ التشخيصي** — ما يصحبه رمز ثابت ويقرؤه من يصلح — وهو
   عربي/إنجليزي «حالياً» بقرار المالك، بقاعدة التصنيف في §6 من القرار نفسه.
6. **لا نسب أو حدود ثابتة في الكود** (ضريبة، محتجز، حد اعتماد) — كلها في جداول إعدادات.
7. **لا مفاتيح أو أسرار في المستودع.** مفاتيح ZATCA في خزينة مفاتيح، والإعدادات عبر متغيرات بيئة.
8. **كل اختبار يُنشئ الحالة التي يفحصها.** اختبارٌ يقرأ ما كتبه اختبار آخر يمرّ بترتيب
   التنفيذ لا ببنائه، فينقلب أحمر عند أول إعادة تسمية أو إضافة اختبار أو تقسيم للمجموعة.
   ولا تُحلّ المشكلة بتجهيزة مشتركة تبذر للجميع — ذلك يُبقيه عاجزاً عن العمل وحده.
   الحارس `tools/test-isolation/run.sh`، والعطل مسجَّل في
   [`docs/evidence/traps.md`](docs/evidence/traps.md#fakh-green-by-ordering-not-by-construction).

## 4. مراجعة الـ Pull Request

قائمة تحقق قبل الدمج:

- [ ] الفرع متفرّع من `develop` ومحدَّث عليه
- [ ] رسائل الالتزام بالصيغة المعتمدة
- [ ] الاختبارات تمر **كاملةً ومفكَّكةً**: ‏`dotnet test --solution Babel.slnx` و`tools/test-isolation/run.sh`
- [ ] أي منطق ترحيل جديد مغطّى باختبار
- [ ] لا أسرار ولا بيانات إنتاج في التغييرات
- [ ] الوثائق المتأثرة محدَّثة (خاصة [مصفوفة الترحيل](docs/reference/posting-matrix.md))
- [ ] أي انحراف عن قرار معماري موثّق موضَّح في وصف الـ PR
- [ ] **لا معرّف محجوز مسبقاً.** الفخّ الجديد يُكتب `فخ-جديد` بمفتاح نصّي
      ([`traps.md` §0.0](docs/evidence/traps.md))، والقرار الجديد يُسمّى ملفه
      `ADR-جديد-اسم-المفتاح.md` وترويسته `# ADR-جديد: …`
      ([`decisions/README.md` §0.0](docs/decisions/README.md)). **الرقم يخصّصه من يُنزل
      الدمج، لا المؤلف** — والحجز المسبق يُنتج فجوة تُبقي الحارس أحمر بلا سبب
      ([فخ-65](docs/evidence/traps.md#fakh-pre-allocated-identifiers-keep-a-guard-routinely-red))
- [ ] **وعند الإنزال:** أُخذ الرقم التالي، وأُعيدت تسمية الملف، وصُحّحت كلمة العدد في
      الفهرس، وحصيلة `traps.md` وكل مجموع مُعاد في وثيقة شقيقة — وإلا سقط
      `AdrRegisterIsSelfConsistent` أو `TrapRegisterIsSelfConsistent`
- [ ] **نصّ جديد يُعرَض لمستخدم؟** صُنِّف بـ[ADR-0021 §6](docs/decisions/ADR-0021-arabic-is-the-record-translation-is-presentation.md):
      تشخيصي (يصحبه رمز ثابت، وقارئه **يصلح**) ⇒ عربي/إنجليزي؛ نصّ عرض (قارئه **يقرّر**)
      ⇒ اسم عربي إلزامي وترجمات صفوفاً. **وعند الشكّ: نصّ عرض**
