# سجل قرارات المعمارية (ADR)

> **الغرض:** أن يستطيع مهندس كفؤ ينضمّ بعد سنتين بلا أي سياق أن (1) **يُعيد** أي قرار **دون
> إعادة تشغيل أي قياس**، لأن كل رقم يحمل منهجه وعتاده والاختبار الاستكشافي الذي أنتجه؛
> (2) لا يعيد اقتراح فكرة **جُرّبت ودُحضت**؛ (3) لا يقع في عطل **اكتُشف من قبل**؛
> (4) يعرف بالضبط أي الادّعاءات **غير مُتحقَّق منها** ولا يجوز البناء عليها.

> **تعليمة المالك الدائمة:** **لا شيء ثابت** إلا أنه خدمة محاسبية تُباع بالاشتراك.
> لا يُكتب أي قرار هنا كعقيدة نهائية. كل قرار يحمل قسم **«ما ينقض هذا القرار»**، وهو —
> لا القرار نفسه — الجزء الأهم في الوثيقة: على الفريق المستقبلي أن **يفحص المُحفِّز** بدل
> أن يُعيد التحليل.

## السجل يتكوّن من ستّ وثائق — وهذه واحدة منها

> **هذه الوثيقة فهرس، لا مرجع.** الجداول المُلخِّصة أدناه (المدحوضات، والأعطال، ودَين التحقّق)
> **مختصرات للمسح السريع**؛ التفصيل والوسم والمصدر في الوثيقة المشار إليها، وهي **المرجع
> المُعتمَد عند أي اختلاف**.

| الوثيقة | ما فيها | متى تُفتَح |
|---|---|---|
| **`decisions/README.md`** (هذه) + وثائق القرارات | القرارات، وكلٌّ منها بـ**ما ينقضه** | حين تسأل «لماذا هذا الشكل؟» أو «هل ما زال صحيحاً؟» |
| [`../evidence/README.md`](../evidence/README.md) | **فهرس قاعدة الأدلة** وترتيب القراءة، و**القواعد الأربع لكل مسار كتابة** | أول ما يُفتَح من جهة الأدلة |
| [`../evidence/measurements.md`](../evidence/measurements.md) | **كل رقم بشروطه وعتاده وأداة قياسه** — **استشهد به بدل إعادة القياس** | قبل كتابة أي أداة قياس جديدة، وقبل أي التزام سعة |
| [`../evidence/traps.md`](../evidence/traps.md) | **‏66 عطلاً، 44 منها صامت**، ولكلٍّ وقاية واختبار — و**§8 قائمة ما قبل الدمج** | **قبل** كتابة شيفرة ترحيل أو إسقاط أو تجزئة، وفي كل مراجعة شيفرة |
| [`../evidence/refuted.md`](../evidence/refuted.md) | **16 ادّعاءً مدحوضاً**، وما بقي من كلٍّ منها، و**ما يعيد فتحه** | حين تُطرح فكرة تبدو جديدة — ابحث عنها هنا أولاً |
| [`../evidence/verification-debt.md`](../evidence/verification-debt.md) | **23 مجهولاً لا يُبنى عليها**، بحقولها الستّة | قبل أي عقد أو تسعيرة أو محادثة عميل |

> **ونقطة الدخول العليا للسجل كلّه:** [`../RECORD.md`](../RECORD.md).

## مستويات الأدلة — القاعدة الأهم في هذا السجل

كل بند دليل في كل ADR موسوم بواحد من هذه الأربعة، **ولا خلط بينها ولا وسوم مركّبة**:
**مقيس** · **مُتحقَّق من مصدر** · **مُستنتَج** · **غير مُتحقَّق منه**.

> **التعريفات المُلزِمة بنصّها الكامل — وشروط كلٍّ منها — في
> [`../evidence/measurements.md` §0](../evidence/measurements.md).** لا تُعاد هنا عمداً:
> تعريفٌ مكرَّر في خمسة مواضع ينحرف في أحدها.
>
> باختصار: **مقيس** = نُفِّذ على جهاز هذا المشروع وقُرئ من مُخرَج التشغيل ·
> **مُتحقَّق من مصدر** = جُلب من مصدر خارجي **مُسمّى دائماً** ·
> **مُستنتَج** = استدلال · **غير مُتحقَّق منه** = **لا يجوز البناء عليه**.

> **رقم موسوم «مقيس» وهو في الحقيقة مُقدَّر هو أسوأ عيب ممكن في هذا السجل**، لأن الغرض كلّه
> هو إعفاء المستقبل من إعادة القياس. **ولا يُذكر مسار ملف بجانب وسم «مقيس» إلا إن كان ذلك
> الملف يُنتج الرقم عند تشغيله.**

## قاعدة الأدلة القابلة لإعادة التشغيل

فرعان يحملان الاختبارات الاستكشافية. **تُقرأ بـ`git show`، ولا تُبدَّل إليها في مساحة عمل قائمة:**

```bash
git show claude/spike-no-marten:spikes/relational-stack/VERDICT.md
git show claude/spike-no-marten:spikes/relational-stack/README.md
git show claude/spike-dotnet10-marten-wolverine:spikes/dotnet-stack/README.md
git ls-tree -r --name-only claude/spike-no-marten -- spikes/
```

> **⚠️ تجاوُز مُسجَّل — يُقرأ قبل فتح `VERDICT.md`:** البند **§5.7** فيه يوصي بكتابة جدول نقاط
> تفتيش (`projection_checkpoint(name, last_event_id, updated_at)`) **قبل** أول إسقاط غير
> متزامن. **هذه التوصية متجاوَزة بدليل لاحق أقوى**: سباق نقطة التفتيش **أُعيد إنتاجه** وفقد
> **100.0000 ريال بصمت** ([`../evidence/traps.md` فخ-01](../evidence/traps.md))، ولذلك
> [ADR-0005](ADR-0005-no-hand-rolled-projector.md) يمنع كتابة مُسقِط يدوي **منعاً باتاً**.
> **اقرأ `VERDICT` للأرقام وللجرد الثماني، لا لهذا البند.**
> وبالمقابل: **§5.6** (نشر بصمة رأس السلسلة يومياً إلى العميل وإلى شاهد خارجي) **قائم
> ومُتبنّى** في [ADR-0007](ADR-0007-journal-entry-hash-chain.md).

لإعادة التشغيل فعلياً (يتطلّب PostgreSQL محلياً وقاعدة بيانات تجريبية — الاختبار **يحذف ويعيد
إنشاء** مخططيه في كل تشغيلة):

```bash
git worktree add /tmp/relspike claude/spike-no-marten
cd /tmp/relspike/spikes/relational-stack && ./run.sh          # كل الإثباتات أ–هـ + جدول الإنتاجية
cd /tmp/relspike/spikes/relational-stack && ./run.sh --only=B  # قسم واحد
cd /tmp/relspike/spikes/relational-stack && ./versions.sh      # جدول الحزم المحلولة فعلياً
```

> **دين توثيقي معلوم — أول عمل هندسي في هذا المسار:** بعض أهم الأرقام في هذا السجل **مقيسة
> فعلاً** لكن **مشغّلاتها لم تُودَع في المستودع**: قياس إنتاجية الترحيل بالنداء الواحد، وتجربة
> أشواط الذهاب والإياب تحت القفل (‏ADR-0010)، وتجربة ترتيب الأقفال والجمود (‏ADR-0004)، وسباق
> نقطة التفتيش (‏ADR-0005)، و11 حالة secp256k1 (‏ADR-0011). كل واحدة موسومة بذلك في مكانها.
> **إيداعها بجانب `spikes/relational-stack/` يجب أن يسبق أي بناء يعتمد عليها**، وإلا فقدنا
> القدرة على إعادة التحقّق بلا إعادة القياس — وهو نقيض الغرض من هذا السجل.

> **دَين توثيقي ثانٍ — المصادر الثانوية غير مُسمّاة بدقّة:** عدد من البنود الموسومة
> **مُتحقَّق من مصدر** في ملفّ الهيئة والسوابق الدولية جاءت من **قراءة فعلية** لتطبيقات
> مفتوحة المصدر على GitHub ولوثائق فنّية ثانوية، **لكن معرّفات المستودعات ومسارات الملفات لم
> تُسجَّل وقت القراءة**. الوسم يبقى صحيحاً (المادة جُلبت فعلاً)، **لكن إعادة الجلب اليوم تتطلّب
> بحثاً من جديد**. القاعدة للمستقبل — وهي مطبَّقة بالفعل في
> [ADR-0005](ADR-0005-no-hand-rolled-projector.md) دليل 3 (‏#4953 و#5108 و#5109):
> **كل مصدر يُسمّى بمعرّفه الدقيق وقت قراءته، لا لاحقاً.**
> والبنود التي لم يُمكن تسمية مصدرها إطلاقاً **أُنزل وسمها** إلى **غير مُتحقَّق منه**
> وأُضيفت إلى [`../evidence/verification-debt.md`](../evidence/verification-debt.md)
> (مثال: [غ-2](../evidence/verification-debt.md) — نمط الفشل الغالب في e-Defter التركي).

## ‏0.0 · قاعدة تخصيص المعرّفات — تُقرأ **قبل** إضافة أي قرار

> **المعرّف الدائم للقرار هو مفتاحه النصّي (`slug`) — ذيلُ اسم ملفه؛ والرقم عرضٌ وترتيب فقط،
> و‍يُخصَّص عند الإنزال لا عند التأليف.**

**لماذا هذه القاعدة موجودة:** الترقيم التسلسلي وقت التأليف **تصادم خمس مرات** في هذا المستودع:
فرعان طلبا فخ-38 وفخ-39، وفرعان طلبا «القاعدة 10»، وفرعان طلبا فخ-41، وفرعان أنشأ كلٌّ منهما
`ADR-0016` — أحدهما «هوية الترحيل تشمل رمز الحدث» والآخر «سطح HTTP عقدٌ منشور» — ودمجهما
`git` **بلا تعارض واحد** لأن اسمَي الملفين مختلفان، ثم ثلاثة فروع متوازية كان كلٌّ منها على
وشك أخذ «الرقم التالي المتاح». والسبب ليس إهمالاً بل **بنية**: رقمٌ يختاره المؤلف يفترض معرفةً
بما تفعله الفروع الأخرى، وهي معرفة **لا يملكها أحد وقت التأليف**.

والعطل **عُولج مرّةً واحدة علاجاً سليماً** — في السجلّ الشقيق وحده
([`../evidence/traps.md` §0.0](../evidence/traps.md)) — **ولم يُنقل إلى هنا**، فتكرّر هنا
حرفياً وهو مُوصَّف هناك (‏[فخ-52](../evidence/traps.md#fakh-a-convention-guarded-in-one-register-and-not-in-its-sibling)).
هذا القسم هو نقل العلاج.

**والمُسكِّن المُستعمَل حتى الآن يزيد العطب:** منسّقٌ يوزّع أرقاماً متمايزة على الفروع مسبقاً.
هو يمنع التصادم فعلاً، لكنه **يُنتج فجوة على كل فرع بحكم البناء**: الفرع الذي أُعطي `0026` لا
يحمل `0025`، فيسقط عنده فحص «الأرقام متّصلة» حتى تهبط الفروع كلّها. **وحارسٌ أحمر لسبب لا
علاقة له بما يحرسه يُدرّب الناس على تجاهله** — وذلك أسوأ من غيابه، لأن الغياب معلوم والتجاهل
ليس كذلك. القاعدة أدناه تُلغي الحاجة إلى التوزيع المسبق أصلاً
(‏[فخ-65](../evidence/traps.md#fakh-pre-allocated-identifiers-keep-a-guard-routinely-red)).

### الإجراء — ثلاث خطوات لا رابع لها

**١ · عند التأليف في فرعك.** اختر مفتاحاً وصفياً بحروف لاتينية صغيرة وشرطات
(`[a-z0-9][a-z0-9-]*`، ثمانية محارف فأكثر)، مشتقاً من **مضمون القرار** لا من موضعه في السجل.
ثم سمِّ الملف واكتب ترويسته بلا رقم:

```text
docs/decisions/ADR-جديد-اسم-المفتاح-باللاتينية.md
# ADR-جديد: عنوان القرار
```

وصفّ الفهرس أدناه: `| [جديد](ADR-جديد-اسم-المفتاح.md) | … |`. **لا تكتب رقماً، ولا تحجز رقماً،
ولا تطلب من أحد أن يحجز لك رقماً.**

**٢ · كل إشارة مرجعية تكتبها في فرعك إلى قرارك غير المُنزَل تستشهد بالمفتاح**، أي بالصيغة
`ADR-جديد` والرابط `ADR-جديد-اسم-المفتاح.md`. **ولا تكتب رقماً في أي موضع.**

**٣ · عند الإنزال.** من يُنزل الدمج يفعل ثلاثة أشياء لا رابع لها: يعيد تسمية الملف بالرقم
التالي المتاح، ويستبدل `ADR-جديد` بالرقم في الترويسة وفي صفّ الفهرس، ويصحّح **كلمة العدد**.
فرعان يهبطان في اليوم نفسه يأخذان رقمين متتاليين، **بلا إعادة ترقيم أيّهما**.

> **وكلمة العدد هي السطر الوحيد الذي يتعارض عمداً** حين يهبط فرعان في اليوم نفسه: كلاهما
> يكتب «الخامس والعشرون» وقد صار العدد ستّة وعشرين. **هذا التعارض مقصود ومحروس**:
> `TheStatedCountWordMatchesTheActualCount` يُفشل البناء عليه، فيُصحَّح مرّة واحدة عند الإنزال.
> ولهذا كُتبت كلمة العدد **في سطر مستقلّ قصير** لا داخل خليّة جدول طويلة: تعارضٌ في سطر من
> ستّ كلمات يُحلّ في ثانية، وتعارضٌ في صفّ من ثلاثين كلمة يُحلّ بالنسخ فوق عمل الآخر.

### الرقم بعد تخصيصه ثابت إلى الأبد

**لا يُغيَّر رقمٌ خُصِّص، ولا يُعاد استعمال رقم، ولا يُملأ فراغٌ برقم قرار حُذف.** وعليه فإن كل
إشارة قائمة — `ADR-0016`، والصيغة العربية-الهندية `ADR-٠٠١٦` — تبقى صالحة بلا استثناء.

### الإشارات المرجعية: بالرقم بعد الإنزال، وبالمفتاح قبله — ولماذا يختلف هذا عن سجل المصائد

سجل المصائد جعل **كل** إشارة تستشهد بالمفتاح. **ولم نَنقل ذلك حرفياً، والفرق مقصود ومُبرَّر
بالقياس لا بالذوق:**

| | سجل المصائد | سجل القرارات |
|---|---|---|
| وحدة العنونة | **قسمٌ داخل ملف واحد**، عنوانه مرساة يخترعها المؤلف بحرّية | **ملفٌّ مستقلّ**، عنوانه مساره — والمسار يحمل الرقم |
| كلفة نزع الرقم من العنوان | صفر: تُضاف مرساة مفتاح بجانب مرساة الرقم، ولا تُبطَل واحدة | **190 رابط ماركداون يحمل اسم الملف، و811 إشارة رقمية في 116 ملفاً** — كلها تُبطَل (مقيس، الأمر أدناه) |
| كم إشارة توجد للقرار **وقت تخصيص رقمه** | إشارات من كل المستودع، لأن الفخّ يُستشهد به في الشيفرة | **ملفّان اثنان لا غير**: الوثيقة نفسها (وهي تُعاد تسميتها على أي حال) وصفّ الفهرس (مقيس على آخر خمسة قرارات) |

**الخلاصة:** الكلفة التي اخترعت قاعدةُ المصائد لإزالتها — تحرير الإشارات وقت التخصيص —
**لا توجد هنا إلا في نافذة الفرع وحدها، وقياسها ملفّان**. أما بعد الإنزال فالرقم ثابت أبداً،
فالإشارة إليه ثابتة مثل الإشارة إلى المفتاح تماماً. **ونزعُ الرقم من أسماء الملفات ليجوز
الاستشهاد بالمفتاح وحده هو البديل المرفوض صراحةً في `traps.md` §0.0** («مفتاح نصّي **بديلاً**
عن الرقم — يُبطل كل إشارة رقمية قائمة»)، وثمنه هنا **مقيس**: 190 رابطاً و811 إشارة.

```bash
# الأرقام أعلاه تُعاد بهذه الأوامر حرفياً، من جذر المستودع:
grep -rIoE '\]\(\.{0,3}[a-zA-Z/.]*ADR-[0-9]{4}-[a-z0-9-]+\.md' . | grep -vE '^\./\.git/|/bin/|/obj/' | wc -l   # 190
grep -rIoE 'ADR-[0-9]{4}' . | grep -vE '^\./\.git/|/bin/|/obj/|node_modules' | wc -l                          # 811
grep -rIlE 'ADR-[0-9]{4}' . | grep -vE '^\./\.git/|/bin/|/obj/|node_modules' | wc -l                          # 116
for n in 0020 0022 0023 0024; do grep -rIl "ADR-$n" . | grep -vE '^\./\.git/|/bin/|/obj/' | wc -l; done       # 2 3 2 2
```

**والمفتاح مع ذلك هو المعرّف الدائم، لا زينة:** هو ما يُكتب في الفرع قبل وجود رقم، وهو ما
يبقى ثابتاً في اسم الملف بعد الرقم، وهو المفروض فريداً ومُطابقاً للشكل بالحارس. ولو اضطُرّ
أحدٌ يوماً إلى تغيير رقم، فالوثيقة **لا تفقد هويتها** لأن هويتها في المفتاح.

### لماذا هذا الشكل دون غيره

| البديل | لماذا رُفض |
|---|---|
| توزيع أرقام على الفروع مسبقاً (المُسكِّن الحالي) | يمنع التصادم و**يُنتج فجوة على كل فرع بحكم البناء**، فيبقى حارس الاتّصال أحمر لأسبوع لسبب لا يخصّه — وهو تدريبٌ على تجاهل الحارس |
| مدى أرقام لكل فرع (‏0030–0039…) | الصورة نفسها أوسع: يتطلّب سجلّ تخصيص يعرفه الجميع — أي المعرفة التي افترضنا غيابها — ويُنتج **فجوات دائمة** |
| نائب مؤقّت بلا مفتاح (`ADR-جديد.md` وحده) | مفتاحان لوثيقتين في فرعين ⇒ اسم ملف واحد ⇒ **تعارض دمج حقيقي**، وهو أهون من التصادم الصامت لكنه يوقف الفرعين معاً بلا داعٍ |
| معرّف مشتقّ من المحتوى (تجزئة) | غير مقروء ولا يُقال في مراجعة: `ADR-9f3a1c` لا يُذكر في نقاش، ويتغيّر مع أول تحرير |
| مفتاح نصّي **بديلاً** عن الرقم في اسم الملف | يُبطل **190 رابطاً و811 إشارة** (مقيس أعلاه). **مرفوض بشرط صريح** — وهو الشرط نفسه في `traps.md` §0.0 |

> **والحارس ليس هذه الفقرة.** `AdrRegisterIsSelfConsistent` في
> `tests/Babel.ArchitectureTests/` **يُفشل البناء** على: رقم مكرَّر · فجوة في الترقيم ·
> مفتاح مكرَّر أو أقصر من ثمانية محارف أو مخالف للشكل · ترويسة داخلية تخالف اسم ملفها ·
> وثيقة على القرص خارج الفهرس أو صفّ فهرس بلا وثيقة · كلمة عدد تخالف العدّ الفعلي ·
> إشارة إلى قرار لا وجود له **بأي من صيغ الأرقام الثلاث** (لاتينية، وعربية-هندية، وفارسية
> موسّعة) · وغياب هذه القاعدة نفسها من صدر الفهرس.
>
> **وما لا يفرضه الحارس تفرضه بوّابة التكامل المستمر:** لا يصل `ADR-جديد` بلا رقم إلى
> `develop`. (البوّابة النظيرة للمصائد قائمة في `.github/workflows/ci.yml`؛ ونظيرتها
> للقرارات **مطلوبة ولم تُودَع بعد** لأن الملف خارج ملكية هذا الفرع — نصّها المقترح في
> رسالة الإنزال.)
>
> **اصطلاحٌ لا يستطيع أحد مخالفته أثمن من اصطلاح يُقال للناس.**

## الفهرس

عدد القرارات في هذا السجل: **الأربعة والعشرون ADR**.
<!-- سطرٌ مستقلّ عمداً — §0.0: هو الموضع الوحيد الذي يتعارض فيه فرعان يهبطان في اليوم نفسه،
     ويصحّحه من يُنزل الدمج، ويحرسه TheStatedCountWordMatchesTheActualCount. -->

| # | العنوان | الحالة | التاريخ | الملخّص في سطر | المُحفِّز الذي ينقضه |
|---|---|---|---|---|---|
| [0001](ADR-0001-technology-stack.md) | الحزمة التقنية، وMarten غير معتمد | مقبول | 2026-08-24 | ‏.NET 10 + PostgreSQL 16 + EF Core 10 + Wolverine، بنموذج استمرارية واحد وبلا Marten | ثلاثة إسقاطات غير متزامنة ثقيلة أو أكثر، أو تيّارات تتجاوز ~100 ألف حدث، أو شحن Marten وضعاً «إضافة فقط» بصلاحيات قابلة للسحب **مع** افتراضات فاشلة صراحةً |
| [0002](ADR-0002-append-only-relational-ledger.md) | دفتر الأستاذ جدول علائقي يُضاف إليه فقط | مقبول | 2026-08-24 | لا إسقاط عن مخزن أحداث؛ التصحيح بقيد عكسي حصراً؛ ‏`numeric(19,4)` بمقياس 4 | ‏`SUM` على نطاق سنة يتجاوز ثانيتين بعد التقسيم والفهرسة، أو إعلان متطلّب على مستوى الدفتر يفرض شكل ملف مختلفاً |
| [0003](ADR-0003-grant-level-immutability.md) | الحصانة على مستوى الصلاحيات | مقبول | 2026-08-24 | ‏`INSERT`+`SELECT` فقط بدور غير مالك وغير superuser، ومشغّل توازن مؤجَّل إلى الـCOMMIT، وتأكيد في الكود | إلزام قانوني بالحذف من داخل الدفتر لا يُلبّى بالإخفاء أو بفصل البيانات الشخصية؛ أو تجاوز كلفة المشغّل المؤجَّل **10٪** من الإنتاجية |
| [0004](ADR-0004-inline-balance-projection.md) | الأرصدة فورية داخل معاملة الترحيل | مقبول | 2026-08-24 | نداء واحد إلى الخادم، و`INSERT … ON CONFLICT`، وصفوف مرتّبة `ORDER BY account_id`؛ السقف المقيس **3,218.8 قيد/ث** عند 8 كتّاب | بلوغ معدّل الكتابة المستدام لمستأجر واحد نحو **2,000 قيد/ث** (~62٪ من السقف المقيس) — والعلاج الأول تقسيم صفوف الأرصدة لا اللاتزامن. ⚠️ **«الهامش ~110×» مُستنتَج لا مقيس**: مقامه افتراض 20 قيد/ث غير مُتحقَّق منه |
| [0005](ADR-0005-no-hand-rolled-projector.md) | رفض طبقة «السفير» — لا إسقاط أصلاً | مقبول | 2026-08-24 | لا يُكتب مُسقِط يدوي أبداً؛ وإن لزم اللاتزامن للتقارير فخفي Marten لا شيفرتنا | ظهور نموذج مقروء تحليلي لا يُقبَل حسابه عند القراءة ولا في منظر مادي — وعندها **Marten**، لا مُسقِط يدوي |
| [0006](ADR-0006-account-lock-as-business-state.md) | قفل الحساب حالة عمل لا إعلان تأخّر | مقبول | 2026-08-24 | مرفوض كآلية تقنية؛ يبقى فقط كحالة يضبطها إنسان بصلاحية وسبب مسجَّل | نقض ADR-0004؛ وأي بديل عندها يجب أن يكون على مستوى المستأجر لا الحساب المفرد، وبمصدر قِدَم مستقلّ عن الخفي المتأخّر نفسه |
| [0007](ADR-0007-journal-entry-hash-chain.md) | سلسلة بصمات فوق القيود + تثبيت خارجي | مقبول | 2026-08-24 | الرقم والبصمة السابقة **داخل** البايتات المُبصَمة، بنطاق (شركة × نوع مستند × سنة)، والتثبيت الخارجي إلزامي | حاجة إلى إثبات وجود قيد **مفرد** لطرف خارجي، أو بطء التحقّق الخطّي ⇒ **شجرة Merkle لكل فترة** (‏RFC 4998)؛ أو إعلان متطلّب على مستوى الدفتر ⇒ e-Defter يصير المواصفة |
| [0008](ADR-0008-gapless-counter-row.md) | عدّاد بلا فجوات: صفّ عدّاد لكل (مستأجر × سلسلة/دفتر × **سنة مالية**)، لا `SEQUENCE` | مقبول | 2026-08-24 | يُقرأ بـ`SELECT … FOR UPDATE` داخل معاملة الترحيل فيعود الرقم مع التراجع؛ ‏`SEQUENCE` **يتسرّب** عند التراجع | بلوغ **~1,000 قيد/ث** على دفتر واحد (**~96٪** من المقيس عند 8 كتّاب) ⇒ **تضييق النطاق أولاً** لا التخلّي عن انعدام الفجوات؛ أو نصّ تنظيمي يقبل الفجوات المُفسَّرة — **والمكسب حينها ≤1.28× لا 4×** |
| [0009](ADR-0009-tenancy-model-open.md) | العزل: الخيارات مفتوحة | **مفتوح** | 2026-08-24 | **ينقض توصية «نموذج العزل (Tenancy)» في `02-architecture.md`**: «مخطط لكل مستأجر» لم يعد موصىً به؛ ويُبنى «منتج تصدير» مهما كان النموذج | **أقل من ~200 مستأجر بسعر مرتفع ⇒ قاعدة لكل مستأجر؛ أكثر من ~500 بسعر منخفض ⇒ `tenant_id` مشترك + RLS**؛ وإلزام إقامة البيانات قد يحسمه من خارج التحليل |
| [0010](ADR-0010-customer-hosted-database.md) | الاستضافة لدى العميل: خيار مُسجَّل | خيار مُسجَّل | 2026-08-24 | قابلة للتطبيق تحت قاعدة **صفر أشواط ذهاب وإياب تحت القفل**؛ **لا تعاقد على سقف زمن شبكة** | منطق ترحيل لا يمكن التعبير عنه بنداء واحد ⇒ السقف يعود إلى **1/(n×RTT)** = ‏8.0 قيد/ث عند 4 أشواط و30 مللي‌ثانية؛ أو تجاوز عدد العملاء المستضيفين ~10 كنقطة مراجعة |
| [0011](ADR-0011-zatca-one-way-compliance-channel.md) | الهيئة قناة أحادية الاتجاه | مقبول | 2026-08-24 | الترحيل أولاً ثم الإرسال عبر الصندوق الصادر؛ لا قيد مشروط بالمقاصة؛ **لا يُعاد توليد قطعة مختومة** | إلزام تنظيمي بالمقاصة المسبقة لفئة مستندات (يغيّر الترتيب لتلك الفئة فقط)؛ أو نشر مفتاح إحكام موثَّق؛ أو إعلان متطلّبات على مستوى الدفتر |
| [0012](ADR-0012-rejected-zatca-as-general-ledger.md) | **مرفوض**: الهيئة كدفتر أستاذ | مقبول (رفض) | 2026-08-24 | خطأ في الفئة: ست عمليات، صفر `GET`، ولا حساب ولا مدين ولا دائن ولا فترة مالية | **مُحفِّز قابل للفحص:** ظهور **أيٍّ** من مفردات الأستاذ (حساب/مدين/دائن/دليل حسابات/فترة مالية/مركز تكلفة) في سطح واجهة حكومية مُوثَّق، **أو** مسار `GET` يعيد **محتوى** مستند — يُفحص ربع سنوياً مع [`verification-debt.md` غ-1](../evidence/verification-debt.md)، ويُقاس بقائمة الدليل 2 بنداً بنداً |
| [0013](ADR-0013-rejected-invoice-shaped-everything.md) | **مرفوض**: قولبة كل شيء كفاتورة | مقبول (رفض) | 2026-08-24 | يتطلّب اختلاق أطراف مقابلة وتقديم مستندات تخالف الواقع؛ **والظرف وحده مُتبنّى** في ADR-0007 | إتاحة الهيئة نوع مستند غير-توريدي صريحاً ومُوثَّقاً رسمياً ⇒ يُعاد النظر **لتلك الأنواع وحدها** |
| [0014](ADR-0014-module-archive-and-entitlement.md) | الأرشفة تحلّ محلّ إلغاء التركيب | مقبول | 2026-08-24 | فحص آلي سابق للأرشفة، وثلاث حالات `NotEntitled`/`Entitled`/**`ReadOnly`**، و`ReadOnly` **قبل أول عميل يدفع** | إلزام قانوني بالحذف من داخل الدفتر؛ أو قرار صريح مكتوب بتأجيل `ReadOnly` **مع تقدير كلفته**: مراجعة كل مسار كتابة في المنتج |
| [0015](ADR-0015-library-boundaries-provider-pattern.md) | حدود المكتبات ونمط المزوّد | مقبول | 2026-08-24 | واجهات نملكها مشتقّة من حالات استخدامنا، بتنفيذ وهمي لكل حدّ؛ ولا تُجرَّد قاعدة البيانات | **إجابة المالك عمّن يحمل المفتاح الخاص لكل مستأجر** ⇒ تُثبَّت واجهة مزوّد الهيئة؛ أو حدّ لم يُبدَّل خلال سنتين ⇒ يُبسَّط |
| [0016](ADR-0016-posting-identity-includes-the-event-code.md) | هوية الترحيل تشمل **رمز الحدث** | مقبول | 2026-08-24 | المفتاح صار سداسياً بإضافة `event_code`، والسؤال يُطرح داخل `ledger.post_entry` نفسها لا في الفهرس وحده؛ والرمز إلزامي غير فارغ في النموذج وقيد التحقق والإجراء المخزَّن و`PostingPlanner` | ظهور مستند يُنتج حدثين بنفس `event_code` عند نفس الإطلاق ⇒ العلاج تسمية حدثين في المصفوفة لا توسيع المفتاح؛ ولا تعود القيمة الافتراضية `''` بأي حال |
| [0017](ADR-0017-subledger-gateway-shares-the-posting-identity.md) | بوابة الدفتر المساعد تحمل هوية الترحيل نفسها | مقبول | 2026-08-24 | البوابة تسأل سؤال الهوية **قبل** الدفتر، فرمز الحدث في استعلامها وفهرسها ومفتاح حصانتها ومطابقتها؛ والنوع المُختلَق `SalesInvoiceCostOfSales` أُزيل | ظهور مستند يُنتج حدثين بنفس `EventCode` عند نفس الإطلاق والجيل ⇒ العلاج تسمية حدثين في المصفوفة لا توسيع المفتاح؛ أو إلغاء جدول المحاولات لصالح قراءة الدفتر مباشرةً |
| [0018](ADR-0018-http-surface-as-a-published-contract.md) | سطح HTTP عقدٌ منشور | مقبول | 2026-08-24 · مُعدَّل 2026-08-24 | المال والأعداد الصحيحة نصّاً بنحو صارم، والثقافة صريحة عند كل حدّ، والأخطاء RFC 9457 برموز ثابتة، والوثيقة مُولَّدة ومُودَعة ومقارَنة بايتاً بايت بحتمية مُثبَتة؛ و`event` حقل إلزامي بتعديل v1 قبل أول نشر | إثباتٌ بتشغيل أن التوليد ليس حتمياً على منصّة نشر أخرى ⇒ تُقارَن بنية مُحلَّلة لا بايتات؛ أو نشر العقد لأول مستهلك ⇒ تنتهي رخصة التعديل في المكان ويصير كل تضييق v2 |
| [0019](ADR-0019-the-explicit-posting-path-consults-the-matrix.md) | المسار الصريح يستشير المصفوفة، ونصّ التخزين لا يعبر، والمجموع في SQL | مقبول | 2026-08-24 | رمز الحدث يُطلب وجوده في المصفوفة على المسار الصريح كما على مسار القالب (`event_code_not_in_matrix`)؛ وخطأ رفض التخزين يحمل `SQLSTATE` ومعرّف تشخيص فقط والنصّ الخام يذهب إلى سجلّ مبنيِ الحقول؛ ومجموعا ميزان المراجعة وحكم توازنه من `sum()` على `numeric` بـ`grouping sets` في الاستعلام نفسه | ظهور حالة ترحيل مشروعة موثَّقة لا يقابلها حدث في المصفوفة ⇒ **تُضاف بيانات ولا يُضعَّف الفحص**؛ أو وصول الخطأ المجالي إلى مستهلك خارج حدّ HTTP يكفّ عن الوقوع ⇒ يُعاد النظر في حجب النصّ وحده؛ أو تبنّي مخزنٍ بلا جمع عشري مضبوط ⇒ يُعاد فتح بند المجموع وحده |
| [0020](ADR-0020-self-held-issuing-unit-keys.md) | **نحن نحوز المفتاح** — مفتاح خاص لكل وحدة إصدار | مقبول | 2026-08-25 | قرار المالك «نبني ولا نشتري» يحسم شكل الحيازة: مفتاح `secp256k1` لكل وحدة إصدار يُولَّد عندنا ولا يغادر حدّ الحيازة؛ ولا مادة مفتاح خاص تعبر أي حدّ؛ والنطاق مطابق عمداً لنطاق العدّاد وسلسلة البصمات؛ و**أربعة بنود خارج النطاق صراحةً**: وحدات الأمان المادية، وتدوير المفاتيح، وخروج المستأجر، والحضانة نيابةً عن الغير | نصّ تنظيمي يمنع المنشأة من الاحتفاظ بمفتاحها لدى مزوّد برمجيات ⇒ يُعاد فتح الشكل الآخر بالكامل مع تقدير ثمن إعادة تسجيل كل وحدة إصدار؛ أو حادثة انكشاف مفتاح ⇒ يُقدَّم بند HSM والتدوير من «مؤجَّل» إلى «حاجز»؛ أو أول عميل يطلب حيازة مفتاحه بنفسه ⇒ شكل ثالث لم يُدرَس |
| [0021](ADR-0021-arabic-is-the-record-translation-is-presentation.md) | **العربية هي السجلّ، والترجمة عرض** | مقبول | 2026-08-25 · مُعدَّل 2026-08-25 | لغة القيد عربية بحكم النظام السعودي؛ وتعدّد اللغات في طبقة العرض وحدها ويعني قابلية الترجمة إلى **أيّ عدد** لا ثنائية عربي/إنجليزي؛ و**الترجمة لا تدخل البايتات المُجزَّأة ولا الدفتر ولا الشكل القانوني**؛ فالاسم العربي إلزامي والترجمات **صفوف بيانات لا أعمدة مخطّط**؛ والارتداد إلى العربية لا إلى الفراغ ولا إلى المفتاح؛ و**§6 يضيّق البند 2 عن النصّ التشخيصي**: ما يصحبه رمز ثابت ويقرؤه من **يصلح** (‏`Error(code, messageAr, messageEn)`، ورسائل قواعد الحجب، وRFC 9457) يبقى عربياً وإنجليزياً **«حالياً»** بقرار المالك، وما يقرؤه من **يقرّر** يخضع للبند 2 كاملاً — وعند الشكّ فهو نصّ عرض؛ والنطاق المُعاد قياسه **118 ملفاً · 1,744 موضعاً · 38 قيداً** (و«72 · 154» القديم مسحوب لتعذّر إعادة إنتاجه) | نصّ تنظيمي يُجيز مسك الدفاتر بغير العربية ⇒ يسقط بند اللغة وحده ويبقى بندا العرض والتجزئة؛ أو إلزام بإدخال ترجمة داخل بايتات مستند رسمي مُوقَّع ⇒ يسقط بند التجزئة و**يُعاد فتح الشكل القانوني بالكامل**؛ أو كيان لا معنى لاسمه العربي ⇒ يُراجَع إلزام الاسم على ذلك الكيان وحده؛ و**لتضييق §6 مُحفِّزاته وحده**: أول منشأة يعجز موظفوها عن قراءة سبب رفض قيد ⇒ يسقط شقّ أخطاء المحاسبة ويبقى شقّ أخطاء البرمجة؛ أو إلزام بتقديم رسائل الرفض بلغة العميل ⇒ يسقط التضييق كلّه |
| [0022](ADR-0022-generated-client-is-the-only-boundary.md) | **العميل مُولَّد من العقد** — والعزل ببوّابة انحراف | مقبول | 2026-08-25 | كل ما تعرفه الواجهة عن الخلفية مُولَّد من `contracts/openapi/v1.json`: الأنواع والعميل والأنماط وواصفات وقت التشغيل؛ و**بوّابة تُعيد التوليد وتقارن البايتات** تفشل عند اختلاف محرف واحد؛ والمال يصل كائناً ترمي تحويلاته الضمنية جميعاً — لا نصّاً ولا رقماً — لأن `Number` يُتلف `1000000000000.4013` إلى `1000000000000.4012` (مقيس)؛ ومواضع المال تُشتقّ من العقد لا تُكتب بيد | ظهور نوع عشري أصيل في JavaScript يصل إليه `JSON.parse` بلا فقد ⇒ يسقط بند الكائن وحده؛ أو منصّة لا تنطق TypeScript ⇒ مولّد ثانٍ **من العقد نفسه** لا استثناء عليه؛ أو تعدّد إصدارات العقد حيّاً ⇒ عميل لكل إصدار في مجلد مستقلّ |
| [0023](ADR-0023-capability-profile-is-a-closed-set-validated-against-the-matrix.md) | **ملفّ القدرات مجموعة مغلقة تُطابَق بالمصفوفة، والعرض مُشتقّ** | مقبول | 2026-08-25 | الفارق بين عميل وعميل **صفوف بيانات** لا فرع شيفرة: مفاتيح معدودة لكل نوع مستند، وشرط دخول المفتاح أن **يفتح حدثاً في مصفوفة الترحيل**؛ والمطابقة تقع **عند الحفظ** ولا تُتجاوَز بنيوياً (لا مُنشئ عام للنوع الصالح، والمخزن لا يقبل مسودّة)؛ والعرض مُشتقّ من (العقد × الملفّ) بلا تخطيط ولا شروط ولا تعابير؛ وسحب قدرة يُرفض بلا إقرار مسبَّب لأنه يترك رصيد دفتر مساعد لا يُخلَّص | حاجة تجارية موثَّقة بلا حدث ⇒ **يُصمَّم الحدث أولاً ولا يُضعَّف الفحص**؛ أو بلوغ عشرات المفاتيح لنوع واحد ⇒ الجواب أنواع مستندات مستقلّة لا مفاتيح أكثر؛ أو أول طلب لتخطيط الحقول من الخادم ⇒ يُعاد فتح حدّ «العرض مُشتقّ» بالكامل؛ أو إثبات أن الإقرار لا يمنع سحباً على رصيد قائم ⇒ ينتقل الحارس إلى جهة الدفتر |
| [0024](ADR-0024-captured-documents-are-pre-entry-drafts.md) | **المستند الملتقَط مسوّدة ما قبل الإدخال** | مقبول | 2026-08-25 | الرمز أولاً والنموذج ثانياً، فالحقول التي يحملها الرمز تصل **مُصدَّقة** ولا تُقرأ ضوئياً؛ و**المصدر لكل حقل لا للمستند** (`attested`/`read`/`inferred`/`defaulted`/`typed`) ولكلٍّ واجب بشري مشتقّ منه؛ والمسوّدة **عاجزة بنيوياً** عن بلوغ الدفتر لأن الوحدة لا تعرف عقد الترحيل أصلاً، والترقية عبر منفذ تنفّذه الوحدة المالكة؛ والمطابقة الحسابية تسمّي الرقم المختلِف ومقداره والمشتبه به؛ والاقتراح **رمز حدث أو دور من المصفوفة لا رمز حساب** | إلزام تنظيمي بإرسال المستندات إلى جهة معتمدة للقراءة ⇒ تسقط الإقامة داخل العملية؛ أو تغيّر مواصفة الرمز ⇒ يُعاد فتح الفاكّ ومتجهاته وحدهما؛ أو قياسٌ يُظهر أن المحاسب يؤكّد كل حقل بلا قراءة على أي حال ⇒ يسقط مبرّر التمييز بين المصادر — **وهذا القياس لم يُجرَ** |


## ما دُحض ولا يُعاد اقتراحه

جدول مضادّ للتكرار. كل بند هنا **جُرّب أو رُوجع وسقط**؛ إعادة اقتراحه بلا معالجة سبب سقوطه
مضيعة لوقت الفريق.

> **هذا فهرس لا مرجع.** المعالجة الكاملة لكل بند — **صيغته الأقوى**، ومن اقترحه، **ولماذا كان
> مقنعاً**، وما دحضه بوسمه، و**ماذا بقي منه**، و**ما الذي يعيد فتح المسألة** — في
> [`../evidence/refuted.md`](../evidence/refuted.md)، وهي **المرجع المُعتمَد**.
> ورموز البنود هناك **د-01 … د-16** بترتيب R1 … R16 نفسه.

| # | الادّعاء | الحكم | أين التفصيل |
|---|---|---|---|
| R1 | «مخزن الأحداث يمنح الحصانة بفيزياء محرّك التخزين» | **مدحوض** — ‏`mt_events` يحتاج `UPDATE`/`DELETE`؛ الجدول العلائقي يستطيع سحبهما | ADR-0001، ADR-0003  · [`refuted.md` د-01](../evidence/refuted.md) |
| R2 | «وحدة تُركَّب لاحقاً تستطيع إعادة تشغيل الأحداث التاريخية» | **مدحوض** — إعادة التشغيل لا تُظهر بُعداً لم يُلتقَط | ADR-0002  · [`refuted.md` د-02](../evidence/refuted.md) |
| R3 | «إسقاطات Marten لا تكتب إلى جداول علائقية حقيقية» | **مدحوض ثلاث مرات** — ادّعاء من المساعد وكان خاطئاً | ADR-0001  · [`refuted.md` د-03](../evidence/refuted.md) |
| R4 | «تأخّر الإسقاط غير المتزامن p95 ‏19.6 ثانية» | **مدحوض** — قياس مستقلّ: p95 ‏1,236 مللي‌ثانية. **لا يُحمَل إلى الأمام** | ADR-0005  · [`refuted.md` د-04](../evidence/refuted.md) |
| R5 | «عامل واحد يُفرّغ 22,242 صفاً/ث» | **مدحوض** — كانت 22.24 **دفعة**/ث × ‏1000 اسمياً. **لا يُحمَل إلى الأمام** | ADR-0005  · [`refuted.md` د-05](../evidence/refuted.md) |
| R6 | «‏24.54٪ من الدفتر يخرج عن التوازن» | **مدحوض كرقم** — 4.4٪ إلى 23٪ مع انقلاب الإشارة. الاتجاه صحيح، **الرقم لا يُقتبَس** | ADR-0005، ADR-0006  · [`refuted.md` د-06](../evidence/refuted.md) |
| R7 | «‏7,580 تنزيلاً يثبت أن منظومة ‏.NET للهيئة هواية» | **حجّة ضعيفة، مسحوبة** — المُسقِط السليم هو **القدرة** | ADR-0011، ADR-0015  · [`refuted.md` د-07](../evidence/refuted.md) |
| R8 | «‏BouncyCastle لازمة لأن secp256k1 معطوب على ‏.NET» | **مدحوض** — 11 حالة ناجحة على ‏.NET 10.0.111 | ADR-0011  · [`refuted.md` د-08](../evidence/refuted.md) |
| R9 | «‏RTT هو القيد المُلزِم ⇒ تعاقد على سقف 15 مللي‌ثانية» | **مدحوض** — القيد **عدد الأشواط تحت القفل**. الرقم 15 لا يُقتبَس | ADR-0010  · [`refuted.md` د-09](../evidence/refuted.md) |
| R10 | «سقف الصفّ الساخن عبر الشبكة لا يمكن تجميعه دفعياً» | **مدحوض** — نداء واحد إلى الخادم يزيله بالكامل | ADR-0010  · [`refuted.md` د-10](../evidence/refuted.md) |
| R11 | «التصدير المُرشَّح يبرّر قاعدة لكل مستأجر» | **مدحوض كسبب حاسم** — الأسباب السليمة: نطاق الضرر واحتواء البيانات الشخصية والنسخ الاحتياطي والسيادة | ADR-0009  · [`refuted.md` د-11](../evidence/refuted.md) |
| R12 | «الهيئة تصلح دفتر أستاذ» | **مدحوض — خطأ في الفئة** | ADR-0012  · [`refuted.md` د-12](../evidence/refuted.md) |
| R13 | «قولبة كل عملية كفاتورة» | **مرفوض** — والظرف وحده مُتبنّى | ADR-0013  · [`refuted.md` د-13](../evidence/refuted.md) |
| R14 | «مخطط لكل مستأجر هو النموذج الموصى به» (‏`02-architecture.md`، قسم «نموذج العزل») | **مدحوض ومسحوب** — ‏Marten لا يدعمه، و**عدد الترحيلات ينمو خطّياً مع عدد العملاء** | ADR-0009  · [`refuted.md` د-14](../evidence/refuted.md) |
| R15 | «القفل لكل حساب يجعل تأخّر الإسقاط آمناً» | **مدحوض** — «متوازن» ثابتة عابرة للحسابات، والعلم يتأخّر مع ما يصفه | ADR-0006  · [`refuted.md` د-15](../evidence/refuted.md) |
| R16 | «لا يوجد أي مسار قراءة موثَّق لدى الهيئة إطلاقاً» | **مبالَغ فيه** — الادّعاء القابل للدفاع: لا مسار يعيد **محتوى** الفاتورة | ADR-0011  · [`refuted.md` د-16](../evidence/refuted.md) |

## الأعطال الصامتة المكتشفة — «المشاكل الحتمية»

كل عطل هنا **أُعيد إنتاجه**، ولا يظهر في المسار السعيد، ولا يرفع استثناءً.

> **هذا فهرس لا مرجع.** السجل الكامل — **‏66 فخّاً**، ولكلٍّ منها **الأثر، وهل يفشل
> بصوت أم بصمت، ومتى يقع بالضبط، وكيف اكتُشف، والوقاية، والاختبار الذي يجب أن يوجد** — في
> [`../evidence/traps.md`](../evidence/traps.md).
> **والبند التنفيذي الوحيد هو [`traps.md` §8](../evidence/traps.md): القائمة المرجعية قبل
> الدمج.** سطر واحد غير محقَّق فيها = لا يُدمَج.

| # | العطل | أين |
|---|---|---|
| 1 | سباق نقطة تفتيش المُسقِط اليدوي ⇒ **فقدان دائم صامت** (‏107.0000 صارت 7.0000) | ADR-0005  · [`traps.md`](../evidence/traps.md) |
| 2 | `UPDATE` مجرّد على فترة بلا صفوف أرصدة ⇒ صفر صفوف، بلا خطأ، **ويُثبَّت** — عند كل بداية فترة | ADR-0004  · [`traps.md`](../evidence/traps.md) |
| 3 | حارس تسلسل لكل حساب ⇒ فقدان 500 من 1,500 عند وصول خارج الترتيب (مزامنة نقاط البيع) | ADR-0005  · [`traps.md`](../evidence/traps.md) |
| 4 | ‏`SkipApplyErrors`/`SkipSerializationErrors`/`SkipUnknownEvents` افتراضها `true`؛ **ولا تهيئة آمنة للدفتر وللنشر معاً** | ADR-0003، ADR-0005  · [`traps.md`](../evidence/traps.md) |
| 5 | توقّف فجوة Marten: جلسة `idle in transaction` واحدة تجمّد كل النماذج المقروءة، **ومقياس القِدَم يقرأ صفراً** | ADR-0005، ADR-0006، ADR-0010  · [`traps.md`](../evidence/traps.md) |
| 6 | إعادة بناء الإسقاط في مكانه: **20.4 ثانية** من ميزان مراجعة خاطئ بلا خطأ؛ وتضاعف 100 ⇒ 200 | ADR-0005  · [`traps.md`](../evidence/traps.md) |
| 7 | `FlatTableProjection` بربط جزئي ⇒ SQL بـ`UPDATE` فقط، ونموذج مقروء **فارغ يُعلن أنه سليم** | ADR-0005، ADR-0006  · [`traps.md`](../evidence/traps.md) |
| 8 | الإسقاطات القائمة على الزيادة لا تشفي نفسها من حدث مُتخطّى | ADR-0005  · [`traps.md`](../evidence/traps.md) |
| 9 | `FlatTableProjection` لا تصل إلى بيانات الحدث الوصفية — وهي ما يستعلمه المدقّق | ADR-0005  · [`traps.md`](../evidence/traps.md) |
| 10 | مخزن الأحداث يسجّل ما **نجح** فقط — بما يشمل **اقتراحات الذكاء الاصطناعي غير المقبولة** | ADR-0002، ADR-0014  · [`traps.md`](../evidence/traps.md) |
| 11 | `timestamptz` ميكروثانية مقابل ticks بـ100 نانوثانية ⇒ **سلسلة لا تُتحقَّق أبداً** إن لم تُبتَر | ADR-0007  · [`traps.md`](../evidence/traps.md) |
| 12 | مقياس `numeric(19,4)` الثابت يغيّر بايت المقياس في `decimal.GetBits()` | ADR-0002  · [`traps.md`](../evidence/traps.md) |
| 13 | `jsonb` يعيد ترتيب المفاتيح ⇒ `AllowOutOfOrderMetadataProperties` | ADR-0007، ADR-0015  · [`traps.md`](../evidence/traps.md) |
| 14 | EF Core 10 + Npgsql يعيد كتابة عمود jsonb كاملاً عند تعديل متداخل | ADR-0001، ADR-0015  · [`traps.md`](../evidence/traps.md) |
| 15 | قوائم GIN المعلّقة تجعل المخطّط يرفض الفهرس بعد `COPY` حتى `VACUUM ANALYZE` | ADR-0015  · [`traps.md`](../evidence/traps.md) |
| 16 | `SaveChangesAndFlushMessagesAsync()` **يُثبِّت** معاملة EF ثم يُفرغ | ADR-0015  · [`traps.md`](../evidence/traps.md) |
| 17 | `WolverineFx.RuntimeCompilation` وRoslyn ⇒ `UseWolverine()` الافتراضي **يفشل عند الإقلاع** | ADR-0001، ADR-0015  · [`traps.md`](../evidence/traps.md) |
| 18 | `WolverineFx.Http` يحتاج `AddWolverineHttp()` أيضاً | ADR-0001  · [`traps.md`](../evidence/traps.md) |
| 19 | Marten يملك سياسات RLS على جداوله ويُسقط أي سياسة يدوية بصمت؛ وsuperuser يتجاوز RLS دائماً | ADR-0003، ADR-0009  · [`traps.md`](../evidence/traps.md) |
| 20 | `UseWolverineManagedEventSubscriptionDistribution` لا يُجمَع مع توزيع Marten؛ و`UseFastEventForwarding` **بلا ضمان ترتيب** | ADR-0005  · [`traps.md`](../evidence/traps.md) |
| 21 | بصمة مزدوجة عند تمرير نصّ base64 لدالة تُبصِم مجدداً؛ و`binarySecurityToken` = base64 لـbase64 لـDER | ADR-0011، ADR-0015  · [`traps.md`](../evidence/traps.md) |
| 22 | الصندوق الصادر «مرّة على الأقل»؛ وإرسال الهيئة **ليس مُحكَماً**، ولا مفتاح إحكام موثَّقاً | ADR-0011  · [`traps.md`](../evidence/traps.md) |
| 23 | **لا تُعِد توليد قطعة مختومة** — العطل السائد في إنتاج e-Defter، ويظهر بعد سنوات | ADR-0011، ADR-0007  · [`traps.md`](../evidence/traps.md) |
| 24 | فجوة التسلسل تُثبَت **إيجاباً**: فترة فارغة تحتاج توقيعاً خاصاً بها | ADR-0007، ADR-0008  · [`traps.md`](../evidence/traps.md) |
| 25 | مخاطر التوحيد العربي: ضوابط الاتجاه، والأرقام العربية-الهندية، والتطويل وصور الألف ⇒ **عمودان منفصلان** للنصّ الموقَّع والمُطبَّع للبحث | ADR-0007  · [`traps.md`](../evidence/traps.md) |
| 26 | تنسيق الأرقام: `100.00` مقابل `100.0` مقابل `100,00` — ‏`LC_NUMERIC` مختلف ⇒ سلسلة غير قابلة للتحقّق **بصمت** | ADR-0007  · [`traps.md`](../evidence/traps.md) |

## دين التحقّق — **لا يُبنى على أيٍّ من هذا**

> **هذا فهرس لا مرجع.** الثلاثة والعشرون بنداً بحقولها الستّة، وطبقاتها (**حاجز** / مؤثر / تقني غير
> مختبَر / إشارة غائبة)، و**دورة المراجعة الإلزامية**، و**قاعدة الإغلاق** — في
> [`../evidence/verification-debt.md`](../evidence/verification-debt.md).

**لم يُتَح الوصول إلى أي مصدر أوّلي لهيئة الزكاة والضريبة والجمارك (ZATCA) ولا إلى أي نصّ
تنظيمي سعودي — كل النطاقات محجوبة بـ403، وكذلك المصادر الدولية المقارنة.**
**القائمة الكاملة، وما كان كلّ منفذ سيُجيب عنه، وتاريخ آخر محاولة:
[`../evidence/verification-debt.md` §1](../evidence/verification-debt.md)** — وهي **المصدر
الوحيد** لهذه القائمة في السجل، فلا تُنسَخ إلى موضع آخر.

**والجدول أدناه ملخّص؛ الحقول الستّة لكل بند (ما المجهول، ولماذا يهمّ، وما الذي يحجزه، وكيف
يُتحقَّق منه، ومن يجيب، وماذا نفعل حتى ذلك الحين) في
[`verification-debt.md`](../evidence/verification-debt.md). كل ما يلي موسوم «غير مُتحقَّق منه»
ولا يجوز البناء عليه:**

| المجال | البند |
|---|---|
| الفوترة الإلكترونية | فترات حفظ السجلات المحاسبية والفواتير (تعارضت المصادر: 5–10 سنوات)؛ هل تنطبق الفوترة على التوريدات **المعفاة** أصلاً (يحسم دخول آلاف عقود الإيجار السكني)؛ **الفوترة نيابةً عن مالك عقار** — أخطر بند، يحتاج إثبات مفهوم في البيئة التجريبية **قبل أي تقدير أو تعاقد**؛ رمز سبب الإعفاء الدقيق |
| ضريبة القيمة المضافة | معالجة غرامات التأخير ورسوم الشيكات المرتدة وإعادة تحميل المرافق (تعويض أم مقابل إضافي — **سطر الضريبة في تلك القيود موسوم: لا يُبنى قبل التأكيد**)؛ حدود التسجيل؛ توزيع ضريبة المدخلات |
| العقاري | ضريبة التصرفات العقارية: النسبة والوعاء ومن يتحمّلها والإعفاءات، ووجود نظام ولائحة أحدث؛ حدود إعفاء الإيجار السكني (مفروش، قصير الأجل)؛ الاستقطاع على الإيجار لمالك غير مقيم؛ نطاق إلزام «إيجار» ومراحله وشرط رخصة الوساطة وتوافر واجهته البرمجية |
| التشغيل | إقامة البيانات داخل المملكة وأي المناطق السحابية حيّة فعلاً |
| توجّه تنظيمي | هل أشارت الهيئة إلى أي توجّه نحو **مستوى الدفتر** أو ملف تدقيق — بُحث بالعربية والإنجليزية ولم يُعثَر على شيء، **لكن موقعها نفسه كان محجوباً، فهذا دليل ضعيف على الغياب** |
| تقني | تعدد المستأجرين المُدار والمقترن في `WolverineFx.EntityFrameworkCore` 6.29.2؛ ‏Windows CNG لـsecp256k1؛ ‏PostgreSQL 17/18 |

## أسئلة على المالك تحسم قرارات معلّقة

| السؤال | يحسم |
|---|---|
| معدّل الذروة الحقيقي للقيود/ث (عدد الفروع ومسارات نقاط البيع لكل فرع ما زالت مفتوحة في `01-scope-and-gaps.md`) — **كل أرقام الهامش مشتقّة من افتراض 20 قيد/ث** | ADR-0004، ADR-0008 |
| **نموذج نقاط البيع: متصل أم يعمل دون إنترنت** (‏`06-risks-and-decisions.md` **ق-3**، ما زال 🔴 مفتوحاً) — وأسوأ نافذة عمل دون إنترنت بالساعات. **العمل دون إنترنت فرضية عاملة لا التزام محسوم**، وثلاثة مواضع في السجل تعتمد عليها بوصفها فرضية | ADR-0011، ADR-0012 |
| تكامل مباشر مع الهيئة أم مزوّد معتمد، **ومن يحمل المفتاح الخاص لكل مستأجر** — يشكّل واجهة المزوّد **بلا رجعة** | ADR-0015، ADR-0011 |
| هل الاستضافة لدى العميل التزام تجاري حقيقي أم فكرة | ADR-0010 |
| عدد المستأجرين بعد ثلاث سنوات والسعر الشهري المستهدف | ADR-0009 |
| إقامة البيانات داخل المملكة وأي سحابة | ADR-0009، ADR-0010 |
| نموذج حزم الوحدات والاستحقاق | ADR-0014 |

## محفّزات إعادة التموضع — لوحة فحص سريعة

| المُحفِّز | العتبة | يُعيد فتح |
|---|---|---|
| إسقاطات غير متزامنة ثقيلة | **3 أو أكثر** | ADR-0001، ADR-0005 |
| حجم التيّار | **~100,000 حدث** لكل تيّار | ADR-0001 |
| معدّل الكتابة لكل مستأجر | **~2,000 قيد/ث** | ADR-0004 |
| معدّل الكتابة لدفتر واحد | **~1,000 قيد/ث** | ADR-0008 |
| عدد المستأجرين | **<~200** بسعر مرتفع ⇒ قاعدة لكل مستأجر · **>~500** بسعر منخفض ⇒ مشترك | ADR-0009 |
| عملاء يستضيفون قاعدتهم | **~10** كنقطة مراجعة أولى | ADR-0010 |
| إعلان الهيئة متطلّبات على مستوى الدفتر | أي إعلان رسمي | ADR-0007، ADR-0011، ADR-0002 |
| أشواط ذهاب وإياب تحت القفل | **أي عدد > 0** ⇒ السقف = 1/(n×RTT) | ADR-0010 |
| زمن التحقّق الكامل من سلسلة سنة مالية | **5 دقائق** على عتاد الإنتاج ⇒ شجرة Merkle لكل فترة | ADR-0007 — ⚠️ **غير مقيس بعد** |
| كلفة المشغّل المؤجَّل منفرداً | **10٪** من الإنتاجية | ADR-0003 — ⚠️ **غير مقيسة بعد**؛ تحتاج قياس A/B |
| `SUM` على نطاق سنة بعد التقسيم والفهرسة | **ثانيتان** | ADR-0002 — ⚠️ **غير مقيس بعد** |
| توافر مزوّد ختم زمني (‏TSA) داخل المملكة وكلفته | إغلاق [ح-5](../evidence/verification-debt.md) | ADR-0007 — **يُمنع وعد «التثبيت الخارجي» قبله** |

> **أربعة من المُحفِّزات أعلاه لا يوجد لها رقم أساس بعد.** ذلك مُدوَّن صراحةً في
> [`../evidence/measurements.md` §7](../evidence/measurements.md) بوصفه **دَيناً قياسياً**،
> ولا يُملأ بتقدير: **مُحفِّز بلا قياس هو مُحفِّز لا يُفحَص، وذلك أصدق من رقم مخترَع.**

> **تنبيه ختامي:** إسقاط Marten **لا يُخرج المشروع من منظومة JasperFx** — Wolverine يعتمد
> `JasperFx` و`Weasel` من الفريق نفسه وبدورة الإصدار نفسها. مخاطرة المورّد الواحد **قُلِّلت
> ولم تُزَل**، ولهذا وُجد ADR-0015.
